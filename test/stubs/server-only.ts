/**
 * بديل `server-only` في الاختبارات.
 *
 * الحزمة الأصلية ترمي خطأً عمداً حين تُحمَّل خارج مكوّن خادم — وهي حماية
 * صحيحة في الإنتاج، لكنها تمنع اختبار طبقة الطلبات في بيئة node.
 * الاستبدال هنا يقتصر على vitest؛ البناء الحقيقي يستعمل الحزمة كما هي.
 */
export {};
